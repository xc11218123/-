# 小數 / 分數 互轉器（Decimal ↔ Fraction Converter）

這是一個可以在線上進行小數與分數互相轉換的網頁工具，支援：
- 有限小數、小數點括號表示的循環小數（例：`0.(3)`、`0.1(6)`）
- 分數輸入（例：`3/4`, `-7/2`）
- 自動推導計算過程
- 可選「四捨五入到小數點後第一位」的模式

## 使用方式
1. 打開頁面後，選擇轉換方向（小數→分數 或 分數→小數）
2. 輸入想轉換的數值
3. 按下「轉換」按鈕即可看到詳細步驟
